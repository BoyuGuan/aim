# My graduation project


# pre_trian.py 
将模型进行预训练的文件，其中选取了torch.nn.Module里的ResNet18, ResNet50, mobileNetV2，三个模型，在并将该三个模型用cifar的训练集训练50个epoch

## 作用 
可以直接加载预训练好的模型，省却提前训练时间。因为原先的torch.nn.Module模型的pre-train是在ImageNet上作的。

### test
asdasdasd